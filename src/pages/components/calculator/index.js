import React from "react";
import { ReactCalculator } from "simple-react-calculator";

const Calculator = (props) => {
  return <div style={{height: '100%'}}><ReactCalculator /></div>;
};

export default Calculator;
